[package]
name = "tmr-voter"
version = "0.0.0"
edition = "2021"
publish = false

# No rubric dependency: this example uses the comment annotation form,
# which needs nothing in the build graph.
[dependencies]
